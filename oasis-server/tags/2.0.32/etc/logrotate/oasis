/var/log/oasis/*log {
  create
  weekly
  missingok
  notifempty
  sharedscripts
  rotate 10
  nocompress

  postrotate
    [ -f /var/run/oasisd.pid ] && /etc/init.d/oasisd restart >/dev/null 2>&1 || true
  endscript
}

